import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cores primárias
  static const Color primary = Color(0xFF0A2463); // Azul escuro financeiro
  static const Color primaryLight = Color(0xFF1E4DB7);
  static const Color primaryDark = Color(0xFF051542);

  // Cores secundárias
  static const Color secondary = Color(0xFF00C896); // Verde fintech
  static const Color secondaryLight = Color(0xFF33D9AA);
  static const Color secondaryDark = Color(0xFF009970);

  // Cores de acento
  static const Color accent = Color(0xFFFFB703); // Dourado

  // Neutros
  static const Color background = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFEEF1F6);

  // Texto
  static const Color textPrimary = Color(0xFF0D1B2A);
  static const Color textSecondary = Color(0xFF5C6B7A);
  static const Color textHint = Color(0xFF9BA8B4);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status
  static const Color success = Color(0xFF00C896);
  static const Color error = Color(0xFFE63946);
  static const Color warning = Color(0xFFFFB703);
  static const Color info = Color(0xFF1E96FC);

  // Dark Mode
  static const Color darkBackground = Color(0xFF0D1B2A);
  static const Color darkSurface = Color(0xFF1A2D3F);
  static const Color darkSurfaceVariant = Color(0xFF243B52);
}
