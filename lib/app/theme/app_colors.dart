import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Cores primárias
  static const Color primary = Color(0xFF0F3760);
  static const Color primaryLight = Color(0xFF2D6F9F);
  static const Color primaryDark = Color(0xFF071B33);

  // Cores secundárias
  static const Color secondary = Color(0xFF2F80ED);
  static const Color secondaryLight = Color(0xFF8FC1FF);
  static const Color secondaryDark = Color(0xFF1B5BB8);

  // Cores de acento
  static const Color accent = Color(0xFFE6A23C);

  // Neutros
  static const Color background = Color(0xFFF4F7FB);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceVariant = Color(0xFFEAF0F6);
  static const Color outline = Color(0xFFD7E0EA);

  // Texto
  static const Color textPrimary = Color(0xFF102033);
  static const Color textSecondary = Color(0xFF5C6B7D);
  static const Color textHint = Color(0xFF93A2B3);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Status
  static const Color success = Color(0xFF00C896);
  static const Color error = Color(0xFFE63946);
  static const Color warning = Color(0xFFFFB703);
  static const Color info = Color(0xFF357DED);

  // Dark Mode
  static const Color darkBackground = Color(0xFF071B33);
  static const Color darkSurface = Color(0xFF102A47);
  static const Color darkSurfaceVariant = Color(0xFF193D63);
}
